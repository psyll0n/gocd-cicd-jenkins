pipeline {
    agent label any
    stages {
        }
        stage("run") { 
            steps {  
                sh "docker pull gocd/gocd-server:v21.1.0"
            }
        }
    }
}
